# Local: Express API (default in dev if unset). Production (e.g. Vercel): set to your API origin,
# e.g. VITE_API_URL=https://your-backend.vercel.app — required unless you proxy /api on the same host.
VITE_API_URL="http://localhost:4000"
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"
VITE_API_URL=https://your-backend.vercel.app